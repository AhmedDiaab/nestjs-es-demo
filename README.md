# build and run with docker
`docker-compose up -d --build`

# stop application
`docker-compose down`


## Notes
`/dco/` directory contains
  - `docker-compose.yml` includes
    - `elasticsearch:8.8.0` 
    - `grafana:8.3.2`
  - `Dockerfile`

